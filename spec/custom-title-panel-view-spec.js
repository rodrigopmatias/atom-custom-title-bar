'use babel';

import CustomTitlePanelView from '../lib/custom-title-panel-view';

describe('CustomTitlePanelView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
